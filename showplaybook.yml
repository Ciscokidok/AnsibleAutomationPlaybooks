---
- name: Catalyst Switch Commands
  hosts: Catalyst

  tasks:
  - name: Show Version
    ansible.netcommon.cli_command:
      commands: show version
    register: out

  - debug: msg="{{out.stdout}}"
      
